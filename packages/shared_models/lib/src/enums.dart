enum UserRole { customer, barber, admin }

enum BarberStatus { pending, approved, rejected, blocked }

enum BookingStatus { pending, confirmed, onTheWay, started, completed, cancelled }

enum PaymentStatus { held, released, refunded, failed }

enum TransactionType { earning, platformFee, payout, instantWithdrawal, withdrawalFee }

enum DayOfWeek { monday, tuesday, wednesday, thursday, friday, saturday, sunday }
