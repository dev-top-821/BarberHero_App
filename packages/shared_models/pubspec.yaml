name: shared_models
description: Shared data models for BarberHero apps
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.9.0
